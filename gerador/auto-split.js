const dict = `
eu vou descobrir
o que você disse
o que você fez
o que você falou
quantos dias
quantos dias
`

function getTeach(dict) {
  const allLines = dict.split('\n').filter(Boolean)

  const dictScore = {}

  for (let line of allLines) {
  }

  console.log(allLines)
}

getTeach(dict)

function autoSplit() {}
